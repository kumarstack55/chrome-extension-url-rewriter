{
  "name": "chrome-extension-url-rewriter",
  "version": "0.0.1",
  "description": "Rewrite URLs based on user-defined rules.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "build": "tsc --noEmit && node build.js",
    "build:fast": "node build.js",
    "watch": "node build.js --watch",
    "test": "tsc -p src/tsconfig.test.json && node --test extension/**/*.test.js",
    "clean": "rimraf --glob extension/**/*.js extension/**/*.js.map"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.10.5",
    "esbuild": "^0.27.2",
    "rimraf": "^6.1.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "diff": "^8.0.2"
  }
}
